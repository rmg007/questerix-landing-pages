{
  "name": "questerix-landing-pages",
  "version": "0.1.0",
  "description": "Questerix public marketing site — landing pages, articles, and brand assets.",
  "private": true,
  "scripts": {
    "dev": "vite",
    "blog:generate": "node scripts/blog-generate.mjs",
    "build": "node scripts/guard-cloudflare-deploy.mjs && node scripts/enforce-tokens.mjs && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"Linting skipped (no config yet)\" && exit 0",
    "test": "echo \"Testing skipped (no tests yet)\" && exit 0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.0",
    "react-markdown": "^9.0.1",
    "remark-gfm": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^6.0.1"
  }
}
